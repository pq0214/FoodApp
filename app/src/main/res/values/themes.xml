<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black</item> <!-- Change this to your desired primary color -->
        <item name="colorButtonNormal">@color/white</item> <!-- Add this line to change button color -->
    </style>

    <style name="Theme.FoodApp" parent="Base.Theme.FoodApp" />
    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/black</item>
    </style>
</resources>